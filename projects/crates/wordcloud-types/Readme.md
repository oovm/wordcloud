# wordcloud-types

Shared domain types for the [wordcloud](https://github.com/oovm/wordcloud) Rust crates.

`wordcloud` and sibling crates depend on this package for layout, element, theme, and error types without pulling rendering dependencies.
